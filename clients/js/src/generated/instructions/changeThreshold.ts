/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findMultisigSignerPda } from "../pdas";
import { MAGICAN_SOLANA_MULTISIG_PROGRAM_ADDRESS } from "../programs";

export const CHANGE_THRESHOLD_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([146, 151, 213, 63, 121, 79, 9, 29]);

export function getChangeThresholdDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CHANGE_THRESHOLD_DISCRIMINATOR,
  );
}

export type ChangeThresholdInstruction<
  TProgram extends string = typeof MAGICAN_SOLANA_MULTISIG_PROGRAM_ADDRESS,
  TAccountMultisig extends string | AccountMeta<string> = string,
  TAccountMultisigSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMultisig extends string
        ? WritableAccount<TAccountMultisig>
        : TAccountMultisig,
      TAccountMultisigSigner extends string
        ? ReadonlySignerAccount<TAccountMultisigSigner> &
            AccountSignerMeta<TAccountMultisigSigner>
        : TAccountMultisigSigner,
      ...TRemainingAccounts,
    ]
  >;

export type ChangeThresholdInstructionData = {
  discriminator: ReadonlyUint8Array;
  threshold: number;
};

export type ChangeThresholdInstructionDataArgs = { threshold: number };

export function getChangeThresholdInstructionDataEncoder(): FixedSizeEncoder<ChangeThresholdInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["threshold", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CHANGE_THRESHOLD_DISCRIMINATOR }),
  );
}

export function getChangeThresholdInstructionDataDecoder(): FixedSizeDecoder<ChangeThresholdInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["threshold", getU8Decoder()],
  ]);
}

export function getChangeThresholdInstructionDataCodec(): FixedSizeCodec<
  ChangeThresholdInstructionDataArgs,
  ChangeThresholdInstructionData
> {
  return combineCodec(
    getChangeThresholdInstructionDataEncoder(),
    getChangeThresholdInstructionDataDecoder(),
  );
}

export type ChangeThresholdAsyncInput<
  TAccountMultisig extends string = string,
  TAccountMultisigSigner extends string = string,
> = {
  multisig: Address<TAccountMultisig>;
  multisigSigner?: TransactionSigner<TAccountMultisigSigner>;
  threshold: ChangeThresholdInstructionDataArgs["threshold"];
};

export async function getChangeThresholdInstructionAsync<
  TAccountMultisig extends string,
  TAccountMultisigSigner extends string,
  TProgramAddress extends Address =
    typeof MAGICAN_SOLANA_MULTISIG_PROGRAM_ADDRESS,
>(
  input: ChangeThresholdAsyncInput<TAccountMultisig, TAccountMultisigSigner>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ChangeThresholdInstruction<
    TProgramAddress,
    TAccountMultisig,
    TAccountMultisigSigner
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MAGICAN_SOLANA_MULTISIG_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    multisig: { value: input.multisig ?? null, isWritable: true },
    multisigSigner: { value: input.multisigSigner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.multisigSigner.value) {
    accounts.multisigSigner.value = await findMultisigSignerPda({
      multisig: getAddressFromResolvedInstructionAccount(
        "multisig",
        accounts.multisig.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("multisig", accounts.multisig),
      getAccountMeta("multisigSigner", accounts.multisigSigner),
    ],
    data: getChangeThresholdInstructionDataEncoder().encode(
      args as ChangeThresholdInstructionDataArgs,
    ),
    programAddress,
  } as ChangeThresholdInstruction<
    TProgramAddress,
    TAccountMultisig,
    TAccountMultisigSigner
  >);
}

export type ChangeThresholdInput<
  TAccountMultisig extends string = string,
  TAccountMultisigSigner extends string = string,
> = {
  multisig: Address<TAccountMultisig>;
  multisigSigner: TransactionSigner<TAccountMultisigSigner>;
  threshold: ChangeThresholdInstructionDataArgs["threshold"];
};

export function getChangeThresholdInstruction<
  TAccountMultisig extends string,
  TAccountMultisigSigner extends string,
  TProgramAddress extends Address =
    typeof MAGICAN_SOLANA_MULTISIG_PROGRAM_ADDRESS,
>(
  input: ChangeThresholdInput<TAccountMultisig, TAccountMultisigSigner>,
  config?: { programAddress?: TProgramAddress },
): ChangeThresholdInstruction<
  TProgramAddress,
  TAccountMultisig,
  TAccountMultisigSigner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MAGICAN_SOLANA_MULTISIG_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    multisig: { value: input.multisig ?? null, isWritable: true },
    multisigSigner: { value: input.multisigSigner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("multisig", accounts.multisig),
      getAccountMeta("multisigSigner", accounts.multisigSigner),
    ],
    data: getChangeThresholdInstructionDataEncoder().encode(
      args as ChangeThresholdInstructionDataArgs,
    ),
    programAddress,
  } as ChangeThresholdInstruction<
    TProgramAddress,
    TAccountMultisig,
    TAccountMultisigSigner
  >);
}

export type ParsedChangeThresholdInstruction<
  TProgram extends string = typeof MAGICAN_SOLANA_MULTISIG_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    multisig: TAccountMetas[0];
    multisigSigner: TAccountMetas[1];
  };
  data: ChangeThresholdInstructionData;
};

export function parseChangeThresholdInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedChangeThresholdInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { multisig: getNextAccount(), multisigSigner: getNextAccount() },
    data: getChangeThresholdInstructionDataDecoder().decode(instruction.data),
  };
}
